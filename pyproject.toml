[project]
name = "vascucase-ai"
version = "0.2.0"
description = "Eight-case vascular-surgery simulation with validated deterministic scoring and constrained GPT feedback."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
